import 'package:flutter/material.dart';

class SheaDailyChecklist extends StatelessWidget {
  const SheaDailyChecklist({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("DailyChecklist");
  }
}
