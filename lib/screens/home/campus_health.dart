import 'package:flutter/material.dart';

class SheaCampusHealth extends StatelessWidget {
  const SheaCampusHealth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Campus Health");
  }
}
