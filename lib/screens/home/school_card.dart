import 'package:flutter/material.dart';

class SheaSchoolCard extends StatelessWidget {
  const SheaSchoolCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
